query FindSceneFilters($filter: FindFilterType, $scene_filter_filter: SceneFilterFilterType) {
  findSceneFilters(filter: $filter, scene_filter_filter: $scene_filter_filter) {
    count
    scene_filters {
      ...SceneFilterData
    }
  }
}