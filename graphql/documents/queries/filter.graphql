query FindSavedFilters($mode: FilterMode!) {
  findSavedFilters(mode: $mode) {
    ...SavedFilterData
  }
}

query FindDefaultFilter($mode: FilterMode!) {
  findDefaultFilter(mode: $mode) {
    ...SavedFilterData
  }
}

query FindRecommendedFilters {
  findRecommendedFilters {
    ...SavedFilterData
  }
}
